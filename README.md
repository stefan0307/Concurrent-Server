# Concurrent Server

A simple two-client chat application built in Java using virtual threads.

## Files

- `Server.java` — accepts two clients and manages their connections
- `ClientHandler.java` — handles incoming messages and forwards them to the other client
- `Client.java` — connects to the server and sends/receives messages
